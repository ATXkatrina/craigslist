<p>
  <%= comment.text %>
</p>
<p>
  Written by: <%= comment.author %> on <%=Time.at(comment.created_at).strftime("%B %e, %Y at %I:%M %p") %>
</p>
<p>
  <%= link_to 'Delete comment', [comment.post, comment],
      method: :delete,
      data: { confirm: "Are you sure you want to delete this comment?" }  %>
</p><br>
